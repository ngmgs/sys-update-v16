languageCode: ja-jp

baseURL: "https://test-web.wikinist.com/"
title: "私の超速ブログ"
theme: PaperMod

pagination:
  pagerSize: 10

# 検索用設定の追加
outputs:
  home:
    - HTML
    - JSON

enableRobotsTXT: false
buildDrafts: false
buildFuture: false
buildExpired: false

minify:
  disableXML: true
  minifyOutput: true

# --- 追加：投稿ページ（posts）を一括でnoindexにし、サイトマップから除外する ---
cascade:
  - target:
      path: "/posts/**"   # content/posts 以下を指す
    sitemap:
      disable: true
    params:
      robotsNoIndex: true

params:
  env: production
  title: "私の超速ブログ"
  description: "ブログ"
  keywords: [Blog, Portfolio, PaperMod]
  author: "私の超速ブログ"
  DateFormat: "2006年01月02日 15:04"
  defaultTheme: auto
  disableThemeToggle: false

  ShowAllPagesInArchive: true

  ShowReadingTime: false
  ShowShareButtons: true
  ShowPostNavLinks: false
  ShowBreadCrumbs: true
  ShowCodeCopyButtons: false
  ShowWordCount: false
  UseHugoToc: false

  # --- 追加：サマリーでHTML（画像）をそのまま表示する設定 ---
  renderSummaryHTML: true

  homeInfoParams:
    Title: "Hi there 👋"
    Content: "ブログです。"

  socialIcons:
    - name: x
      url: "https://x.com/"
    - name: github
      url: "https://github.com/"

  # 画像の設定（一応残しますが、hhh.py側で制御します）
  cover:
    hiddenInList: false
    hiddenInSingle: false
    relative: false
    linkFullImages: true

menu:
  main:
    - identifier: search
      name: 🔍 検索
      url: /search/
      weight: 5
    - identifier: categories
      name: categories
      url: /categories/
      weight: 10
    - identifier: tags
      name: tags
      url: /tags/
      weight: 20

pygmentsUseClasses: true

# --- 修正：HTMLタグ（画像リンクなど）を許可する ---
markup:
  goldmark:
    renderer:
      unsafe: true
  highlight:
    noClasses: false

taxonomies:
  tag: tags